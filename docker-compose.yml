version: '3.9'

services:
  db:
    image: mysql:8
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: rxinsight
    ports:
      - "3307:3306"
    volumes:
      - ./db:/docker-entrypoint-initdb.d
